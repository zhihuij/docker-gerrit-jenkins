[rule "open"]
    event-type = patchset-created
    action = add-soy-comment Open
    action = 开始进行

[rule "merged"]
    event-type = change-merged
    action = add-soy-comment Merged
    action = 解决问题